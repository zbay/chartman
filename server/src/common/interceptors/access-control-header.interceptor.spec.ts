import { AccessControlHeaderInterceptor } from './access-control-header.interceptor';

describe('AccessControlHeaderInterceptor', () => {
  it('should be defined', () => {
    expect(new AccessControlHeaderInterceptor()).toBeDefined();
  });
});
